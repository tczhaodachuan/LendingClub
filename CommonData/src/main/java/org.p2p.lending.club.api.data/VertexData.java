package org.p2p.lending.club.api.data;

/**
 * Created by tczhaodachuan on 7/1/2015.
 */
public interface VertexData {
    /*
    VertexData interface doesn't override the object equals method.
    Since the signature is VertexData class.
     */
    boolean equals(VertexData vertexData);
}
